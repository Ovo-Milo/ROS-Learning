# 语音采集

[← 返回目录](./README.md)

---

## 实验目的

- 熟悉 robot_audio 功能包中的语音采集服务
- 学习如何启动语音采集服务
- 编写客户端程序采集一段音频

## 依赖安装

```bash
sudo apt install libasound2-dev
sudo apt-get install sox  # 命令行播放软件
```

> 功能包下载：https://pan.baidu.com/s/1j2RNCZLwqX0y5KLndBtWcA （提取码: v0mk）

---

## 服务接口

**服务名**：`voice_collect`

**类型**：`robot_audio/Collect`

```
# Request
bool collect_flag    # 1=采集, 0=不采集
---
# Response
string voice_filename  # 采集到的音频文件路径
```

---

## 实现代码

```cpp
#include <ros/ros.h>
#include <robot_audio/Collect.h>
#include <iostream>
#include <string>

int main(int argc, char **argv)
{
    ros::init(argc, argv, "collect");
    ros::NodeHandle n;

    ros::ServiceClient collect_client = n.serviceClient<robot_audio::Collect>("voice_collect");
    robot_audio::Collect srv;
    srv.request.collect_flag = 1;

    ros::service::waitForService("voice_collect");
    collect_client.call(srv);

    ROS_INFO("File saved in: %s", srv.response.voice_filename.c_str());

    // 播放音频
    std::string dir = "play " + srv.response.voice_filename;
    sleep(1);
    system(dir.c_str());
}
```

---

## Launch 文件

`launch/voice_collect.launch`：

```xml
<launch>
    <!-- 实验节点 -->
    <node pkg="bobac3_audio" type="collect_node" name="collect" output="screen"/>
    <!-- 语音采集节点 -->
    <node name="voice_collect" pkg="robot_audio" type="voice_collect_node" output="screen">
        <param name="audio_file" type="string" value="./AIUI/audio/audio.wav"/>
    </node>
</launch>
```

---

## 运行

```bash
roslaunch bobac3_audio voice_collect.launch
# 对机器人说一段话，机器人会录制并保存音频
```

---

[← 返回目录](./README.md)
